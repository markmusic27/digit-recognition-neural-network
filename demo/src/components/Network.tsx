type NetworkProps = {
  width: number;
};

const Network = (props: NetworkProps) => {
  const originalSize = 710;

  return <div className={`bg-amber-300`}>Network jkjekl</div>;
};

export default Network;
